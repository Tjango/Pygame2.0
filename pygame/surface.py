from sdl2 import surface
from sdl2 import ext
"""
from pygame.display import get_surface as window

def fill(color, rect=None, special_flags=0):
    if rect==None: rect = window()

    print(color)
    color = ext.string_to_color(color)

    try:
        ext.fill(rect, ext.convert_to_color(color))
    except:
        ext.fill(rect, color)
"""
